autoload -U promptinit && promptinit

# PROMPT
prompt pure

# PLUGINS

# syntax highlighting

source ${(q-)PWD}/plugins/syntax/zsh-syntax-highlighting.zsh

# autojump

[[ -s $(brew --prefix)/etc/profile.d/autojump.sh ]] && . $(brew --prefix)/etc/profile.d/autojump.sh
